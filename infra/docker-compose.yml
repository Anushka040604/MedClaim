services:
  postgres:
    image: postgres:16
    container_name: mcv_postgres
    environment:
      POSTGRES_USER: mcv
      POSTGRES_PASSWORD: mcv
      POSTGRES_DB: mcv
    ports:
      - "5432:5432"
    volumes:
      - mcv_pgdata:/var/lib/postgresql/data

volumes:
  mcv_pgdata:

